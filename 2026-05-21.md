# 2026-05-21

## Android 屏幕常亮应用开发

- 用户要求写一个带图形界面的 Android 程序，功能是屏幕常亮不息屏，低功耗
- 创建了完整项目 `ScreenKeeper/`，位于 `/root/.openclaw/workspace/ScreenKeeper/`
- 功能：屏幕常亮（FLAG_KEEP_SCREEN_ON + 前台服务）、低亮度模式、亮度滑块调节、开机自启
- 技术栈：Java, Material Design, targetSdk 34, minSdk 24
- 服务器没有 JDK 和 Android SDK，通过 GitHub Actions 自动编译打包
- GitHub 仓库: https://github.com/ming-a1/ScreenKeeper

## 迭代过程

### v1.0 - 初始版本
- 代码完成，暗色主题

### v1.1 - Switch 颜色修复
- 添加 Switch thumb/track 颜色，解决暗色主题下开关不可见
- 添加 `app:useMaterialThemeColors="false"`
- 修复 GitHub Actions workflow 触发分支 master→main
- Release tag 改用版本名而非 run_number

### v1.2 - 切换亮色模式
- 用户反馈暗色模式下 Switch 还是看不到
- 颜色全面改为亮色：白色背景、浅灰卡片、深色文字
- 状态栏添加深色图标支持（WindowInsetsControllerCompat）

### v1.3 - 强制亮色模式
- 用户设备系统开了暗色模式，Light 主题被系统覆盖
- 添加 AppCompatDelegate.MODE_NIGHT_NO 强制亮色

### v1.4 - 替换 MaterialSwitch 为 SwitchCompat
- 用户反馈"屏幕常亮"开关完全看不到
- MaterialSwitch 在部分设备上渲染异常
- 改用 SwitchCompat + 显式 thumbTint/trackTint

### v1.5 - Android 14 前台服务崩溃修复
- 报错: SecurityException - FOREGROUND_SERVICE_SPECIAL_USE 权限缺失
- 添加 FOREGROUND_SERVICE_SPECIAL_USE 权限
- Service 添加 PROPERTY_SPECIAL_USE_FGS_SUBTYPE 属性
- startForeground() 传入 FOREGROUND_SERVICE_TYPE_SPECIAL_USE

## 待办
- 用户已提供 GitHub Token（明文暴露在聊天中，提醒用户撤销）
- 持续迭代优化

## v1.6 - 后台屏幕常亮（WakeLock）
- 用户反馈前台能保活，但切到后台屏幕就灭了
- KeepAliveService 添加 WakeLock (SCREEN_BRIGHT_WAKE_LOCK | ACQUIRE_CAUSES_WAKEUP)
- 前台服务启动时获取，停止/销毁时释放
- AndroidManifest 添加 WAKE_LOCK 权限
- 30秒定时唤醒保留，功耗可忽略，起防系统回收作用

## v1.7 - 悬浮窗替代WakeLock
- v1.6的WakeLock方案在用户设备上无效，后台仍然息屏
- 改用1x1像素透明悬浮窗 + FLAG_KEEP_SCREEN_ON
- 悬浮窗类型TYPE_APPLICATION_OVERLAY，带FLAG_NOT_FOCUSABLE|FLAG_NOT_TOUCHABLE
- 移除WAKE_LOCK权限
